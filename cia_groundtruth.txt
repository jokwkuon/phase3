# Ground-truth file for CIA evaluation on CIASample.java
# Node counter mapping for compute():
#   0  block (method entry - structural)
#   1  int sum = 0
#   2  int i = 1
#   3  int n = 10
#   4  int threshold = 50
#   5  while (i <= n)
#   6  block (loop body - structural)
#   7  sum = sum + i
#   8  i++
#   9  int result = sum
#  10  if (result > threshold)
#  11  block (then body - structural)
#  12  result = threshold
#  13  return result
#
# Node counter mapping for min():
#  14  block (method entry - structural)
#  15  int a = x
#  16  int b = y
#  17  if (a > b)
#  18  block (then body - structural)
#  19  int temp = a
#  20  a = b
#  21  b = temp
#  22  return a
#
# MANUAL ANALYSIS NOTES:
# Block nodes (0,6,11,14,18) are structural CFG artifacts with no
# corresponding source statement. A human analyst performing CIA
# on the source code would never identify them as impacted statements.
# They appear as FPs in every scenario — a systematic over-approximation
# introduced by the PDG model's treatment of compound statements.

# Change point 2 (int i = 1):
# i is used in: 5(while condition), 7(sum=sum+i), 8(i++)
# 5 controls loop body statements: 7, 8
# 8 feeds back into 5 (loop-carried dep on i)
# 7 feeds 9 (sum->result); 9 feeds 10(if) and 13(return)
# 10 controls 12(result=threshold); 12 feeds 13
# Block nodes 6,11 excluded — structural artifacts, not source statements
CHANGE_POINT: 2
IDEAL: 5 7 8 9 10 12 13

# Change point 4 (int threshold = 50):
# threshold used in: 10(if condition), 12(result=threshold RHS)
# 10 controls 12; 12 feeds 13(return)
# Block node 11 excluded — structural artifact
CHANGE_POINT: 4
IDEAL: 10 12 13

# Change point 7 (sum = sum + i):
# sum defined here feeds: 9(int result=sum)
# 9 feeds 10(if) and 13(return); 10 controls 12; 12 feeds 13
# Block node 11 excluded — structural artifact
CHANGE_POINT: 7
IDEAL: 9 10 12 13

# Change point 17 (if (a > b)) in min():
# 17 controls: 19(int temp=a), 20(a=b), 21(b=temp)
# 20(a=b) redefines a, which feeds 22(return a)
# Block node 18 excluded — structural artifact
CHANGE_POINT: 17
IDEAL: 19 20 21 22
